இறக்குமதி "../../std/io.agam";


செயல் sum(slice: &[எண்]): எண் {
    மாறி நிலை s: எண் = 0;
    சுற்று (i உள் slice.len()) {
        s = s + slice[i];
    }
    பதிப்பி(s);
    விடை s;
}

செயல் மைய(): எண் {
    மாறி arr: [எண்; 3] = [1, 2, 3];
    விடை sum(&arr);
}
