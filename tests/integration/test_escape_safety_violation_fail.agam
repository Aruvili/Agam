செயல் test_escape_violation(): வெற்று {
    மாறி நிலை x: *எண்32 = ஒதுக்கீடு<எண்32>(1);
    மாறி b1: *எண்32 = கடன் பகிர்வு(x);
    மாறி y: *எண்32 = தப்பித்தல்(x) -> heap; // ERROR: Cannot தப்பித்தல் variable 'x' வரை it is borrowed
}

செயல் மைய(): எண்32 {
    விடை 0;
}
